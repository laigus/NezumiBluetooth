# 项目约定

## 目录结构

`【不提交 Git】` 表示该路径由 `.gitignore` 排除；`按需创建` 表示当前可以不存在。

```text
NezumiBluetooth/
├─ miniprogram/              # 小程序运行时源码
│  ├─ core/                   # 设备无关的 BLE 与连接会话
│  ├─ devices/
│  │  ├─ index.js             # 设备模块注册表
│  │  └─ <device>/
│  │     ├─ definition.js     # 识别、能力与页面入口
│  │     ├─ profile.js        # GATT 与传输约定
│  │     ├─ controller.js     # 设备协议控制器
│  │     └─ <feature>/        # 【按需创建】该设备的专属功能数据
│  ├─ pages/
│  │  └─ <name>/              # 通用页或设备模块声明的共享页
│  ├─ utils/                  # 设备无关的共享运行逻辑
│  └─ assets/                 # 小程序内使用的图片
├─ tests/                     # 单文件核心回归测试
├─ tools/
│  ├─ coco/                   # COCO 专属本地工具
│  │  └─ extract-frequency.py # COCO 抓包转频率文件
│  └─ <device>/               # 【按需创建】其他设备自己的本地工具
├─ docs/
│  ├─ ARCHITECTURE.md         # 通用架构与设备接入约定
│  ├─ WECHAT_MINIPROGRAM_GUIDE.md # 第一次微信小程序开发指引
│  └─ <device>/               # 具体设备的使用与技术文档
├─ assets/                    # 品牌与发布素材
├─ package.json
├─ project.config.example.json # 可提交的微信项目示例配置
├─ project.config.json       # 【不提交 Git】含本机 AppID 的项目配置
├─ README.md
├─ AGENTS.md
├─ project.private.config.json  # 【不提交 Git】本机开发配置
├─ .tools/                    # 【不提交 Git】本地第三方工具
├─ captures/                  # 【不提交 Git｜按需创建】原始采集文件
└─ analysis/                  # 【不提交 Git｜按需创建】中间分析结果
```

根目录不散放业务代码或临时脚本；新增文件按上图归类，同一职责只保留一个位置。

## 设备模块边界

- 新设备只在 `miniprogram/devices/index.js` 注册一次；定义负责识别规则、能力声明和控制页面入口。
- 模块 ID 用于注册与路由，画像 ID 用于传输配置和设备数据归属；两者职责分开。
- `core/bluetooth.js` 只封装原始 BLE 操作，不导入具体设备；协议编码、握手和停止行为放在设备控制器中。
- 设备专属页面可独立新增；只复用真正相同的能力和页面，不根据已有设备推定新设备的功能。
- 本地工具按设备放入 `tools/<device>/`；每个目录只服务该设备，不建立统一工具入口或工具注册表。
- 设备工具不共享功能假设；是否需要抓包、频率、灯光配置或其他处理，由该设备自身决定，没有本地工具的设备不创建目录。

## 测试边界

- 自动测试只保护 BLE 核心、设备注册表、已注册模块的稳定行为和运行时文件完整性，统一收敛在 `tests/run-tests.js`。
- 设备专属能力只由对应模块的最小测试覆盖，不把它提升为所有设备必须满足的公共契约。
- 界面文案、品牌文字、文档内容、本机绝对路径、测试数量和易变布局细节不作为自动断言。
- 测试数据优先使用最小合成样本，不提交真实采集时间戳、设备标识或仅供一次排查的数据。
- 只有可稳定复现的核心缺陷才新增回归测试；避免重复验证同一行为、照抄实现逻辑或为覆盖率堆测试。

## 维护规则

- 以当前工作区为事实来源；保留人工修改，在其基础上继续，不按历史记忆覆盖文件。
- 功能或设计调整必须同步整理实现、测试和相关文档，确保名称、行为与流程一致。
- 根目录 README 只描述项目定位、通用能力和文档入口；通用架构与开发流程放在 `docs/` 根层。
- 设备使用、协议流程和设备工具说明放在 `docs/<device>/`；具体频率名称、ID、帧内容、采集时间和单次分析结果仍只留在源码或本机数据中。
- 未明确要求兼容时，只保留现行方案，删除旧实现、旧入口、旧流程及过时说明。
- 中间过程文件仅在任务期间保留；确认不再用于当前流程或验证后及时删除。
- 变更历史统一交给 Git 管理，不在项目中保存源码快照、补丁副本或专用恢复脚本。
- 只维护当前实现；修改直接更新现有代码和文档，不创建发布编号、变更日志或升级流程。
- 改动保持聚焦；不提交临时文件、缓存或一次性调试产物，结束前完成与改动相符的验证。

## 公共仓库与敏感信息

- 公开仓库只提交示例配置；真实 AppID、AppSecret、账号、令牌和开发者工具私有配置只保存在被 Git 忽略的本机文件中。
- 原始抓包、设备 ID、广播数据、时间戳、输入文件名、文件摘要、源帧号、完整调试日志和单次分析记录只放在 `captures/`、`analysis/` 或其他被忽略的本机位置。
- 可分享文件和本地工具默认只输出运行必需字段，不携带本机来源与采集溯源信息。
- 图片提交前清除 EXIF、XMP、文本块、作者、生成任务 ID、提示词、创建时间和本机路径等元数据。
- 新增脚本时检查默认输出路径和内容；本机诊断产物必须被 `.gitignore` 覆盖，不在根目录生成可误提交的文件。
- BLE 协议、控制字节和设备功能不属于个人隐私，但公开即代表主动披露；需要保密的设备模块应放在私有仓库，而不是依赖客户端混淆。
- 首次公开推送前检查暂存区与完整 Git 历史；敏感内容进入历史后，应在推送前重建干净历史。
